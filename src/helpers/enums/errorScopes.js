const errorScopes = {
  NOT_FOUND: 'NOT_FOUND',
  CONFLICT: 'CONFLICT',
  INVALID_CREDENTIALS: 'INVALID_EMAIL_OR_PASSWORD',
  INACTIVE_ACCOUNT: 'Inactive account'
};

module.exports = Object.freeze(errorScopes);
